import {
	GET_CITY_REQUEST,
	GET_CITY_FAILURE,
	GET_CITY_SUCCESS,
} from '../constants/ActionTypes';

const initialState = {
	cities: [],
	error: null,
};

export default (state = initialState, action) => {
	switch (action.type) {
		case GET_CHATS_REQUEST:
	}
};
